# Instalar librerias
flutter pub get

# Buscar dispositivos que flutter detecta
flutter devices

# Imprimir errores que flutter detecta
flutter doctor

# Indicarle al celular que redirija las peticiones a la computadora
adb reverse tcp:8000 tcp:8000

# Ejecutar el proyecto
flutter run -d <device_id>

# Documentación oficial
https://docs.flutter.dev/
